using UnityEngine;

/// <summary>
/// Legacy scene component retained so existing serialized references remain
/// valid. Production muzzle validation and debug drawing now belong to
/// MuzzleClearanceResolver.
/// </summary>
public class AimMuzzleDebug : MonoBehaviour
{
}
